# Model Checker
Model Checker is an app that runs local AI models on device. It uses ONNX Runtime GenAI to run the models locally and take advantage of a wide variety of hardware acceleration options.
## Supported platforms
Currently Model Checker is Windows only, as it depends on WinUI and Windows APIs to access ONNX Runtime GenAI. There are future plans to expand to Android devices.
